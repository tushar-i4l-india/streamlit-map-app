Follow the below instruction to run the program:

1. Install Required Libraries (streamlit, geopy, folium, streamlit-folium):

pip install streamlit pandas geopy folium streamlit-folium 

2. To run the code, open the folder in VS code editor and run the following command:

streamlit run streamlit-python-app.py

3. Upload the order file in excel format (.xlsx).
